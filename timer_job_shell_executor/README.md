# timer_job_shell_executor

该模块是旧版 XXL-JOB Shell 执行器。

## 当前状态

当前 WSL 本地用户画像主线使用 `user_profile_scripts/run_user_profile_v2_wsl.sh` 和 `user_profile_visualization/run_visualization_wsl.sh`，不再依赖旧 Shell 执行器。

旧 Shell 执行器保留为原项目调度能力参考，不参与当前本地运行闭环。
