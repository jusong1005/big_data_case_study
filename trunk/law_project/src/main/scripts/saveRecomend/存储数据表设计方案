//表设计，用于存储推荐结果
/**
要求：1、推荐表中应该有一个字段是UID，作为推荐用户的标识
2.为了查询反应快速，不能存放在Hive，可以选择结构化数据库Mysql和非结构化数据库HBase
3.基于MYSQL设计：MYSQL是结构化表
第一种方案是根据推荐的个数创建字段数，再加上userID（主键），因为在推荐模型里面，会设置为用户推荐多少个值。
优点：结构规整，易于查询，查询快捷
缺点：如果推荐个数有变，表结构也会变化，对于推荐个数小于指定个数的记录，会有部分字段没有值存储，造成资源浪费
第二种方案是仅设置两个列，一个用户id列，一个推荐列，用户推荐的值依次存入，进行推荐时通过useID查询结果并按照需要提取N个推荐
优点：表结构无需变化
缺点：无法对useID设置主键，数据量大的情况下查询效率略有降低，而且每次如果不重新创建表，表的数据将不断增大
使用MYSQL优点，跟临时存储的Hive能够保持结果一致，跟网站后台使用的数据库保持一致
方案一：
userID（主键）	Rec1	Rec2	………………..
Userid1	Fullurl1	Fullurl2

方案二：
userid	rec
User1	Fullurl1
User1	Fullurl2

4.基于HBase设计：HBase是非结构化表
如果基于HBase，主键为userID，列簇一个，列簇下的列一个即可，版本可以固定设置为10,20,30等，数据存入HBase之后，会根据存入的时间分出先后，每次将前N个推荐给用户即可
优点：有主键，查询快，表结构无需改变，适合存储稀疏数据，不会造成空间浪费
缺点：非结构化数据库，与Hive之间的数据传输比较困难，网站后台用的是MYSQL，这里使用HBase，需要增加一个数据库，跟网站后台数据库不能保持一致。
Userid(rowkey)	Recommend(version=10)
	                rec
Userid1	Fullurl1
Userid2	Fullurl2

结论：经过分析，存储在MYSQL的可以采用方案一，存储在HBase可以采用以上方案
Mysql创建表语句看recommend_mysql
HBase建表语句看recommend_hbase
 */
