一、Sqoop全量导入数据到Hive
通过Sqoop将MySQL数据库law_init的表lawtime_all中的数据导入Hive数据库law_init中的lawtime_all表
sqoop --import --connect jdbc:mysql://192.168.2.162:3306/law_init --username root --password 123456 --table lawtime_all --direct --hive-import --hive-database law_init --hive-table lawtime_all --driver com.mysql.jdbc.Driver --m 20 --split-by userid

二、Sqoop增量更新数据到Hive
第1步：在Shell脚本中创建一个Sqoop的任务，任务的内容是增量导入MySQL的数据到Hive
脚本运行需要输入两个参数，分别是MYSQL数据库名称，一个是MYSQL表名称。数据库的用户名、密码、IP直接写入命令中，导入的Hive数据库的库名跟表名与MYSQL一致，选择的增量更新字段为id，id从1开始，所以选择的起始值为0。

第2步：运行第一步的脚本，查看是否创建任务。
在mysql_to_hive.sh所在文件夹中，确保mysql_to_hive.sh有可执行的权限后，
通过命令“./ mysql_to_hive.sh law_init lawtime_reality”运行脚本，完成后通过命令“sqoop --show law_job”查看任务信息。

第3步：编写执行任务的脚本
任务创建后并不会自动执行，需要通过执行命令执行任务，将执行任务的代码写入脚本中。执行脚本需要输入一个参数，就是日志数据存放的文件路径
exec_job.sh执行脚本代码如下：
#!/bin/bash
log_file=$1
start_time=$(date +%Y-%m-%d-%H:%M:%S)
echo -e "\nstart time : $start_time" >> $log_file
start_time=$(date +%s)
source /etc/profile
sqoop job --exec 'law_job' >> $log_file
end_time=$(date +%Y-%m-%d-%H:%M:%S)
echo "end time : $end_time" >> $log_file
end_time=$(date +%s)
cost_time=$(($end_time - $start_time))
echo -e "cost time: $cost_time seconds \n" >> $log_file

第4步：设置定时任务
在xxl-job中，可以通过Shell执行器设置定时任务，定时运行exec_job.sh脚本，
也可以在Linux中通过命令“crontab -e”进入定时任务设置的文件，在文件中添加命令“0 3 * * * /root/ring/exec_job.sh /root/ring/sqoop.log >> /root/ring/exec_log”，
系统将会在每天3点将lawtime_reality表中新增的数据导入Hive，查看Hive中数据增加的情况即可了解任务运行情况，
另外，也可以通过查看日志文件sqoop.log
