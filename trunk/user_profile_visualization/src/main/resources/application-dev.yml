#开发环境
name: media

server:
    context-path: /user_profile
    port: 8001
    config:
      auth:
#是否启用权限验证
        enabled: false
    session:
      cookie:
        domain: localhost

#loginUrl



spring:
  datasource:
    url: jdbc:mysql://192.168.111.75:3306/user_profile
    username: root
    password: root
    driver-class: com.mysql.jdbc.Driver
#    name: datasource
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
  thymeleaf:
  #    prefix: classpath:/templates/
  #    suffix: .html
  #    enabled: true
    mode: LEGACYHTML5

  session:
    store-type: none



