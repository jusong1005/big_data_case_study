#开发环境
name: media

server:
    context-path: /user_profile
    port: 8001
    config:
      auth:
#是否启用权限验证
        enabled: false
    session:
      cookie:
        domain: localhost

#loginUrl



spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zjsm?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class: com.mysql.jdbc.Driver
#    name: datasource
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
  thymeleaf:
  #    prefix: classpath:/templates/
  #    suffix: .html
  #    enabled: true
    mode: LEGACYHTML5

  session:
    store-type: none



